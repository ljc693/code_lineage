# Disable heavy startup components for tests
lineage.dataloader.enabled=false
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Keep Neo4j + MySQL for integration tests
# (integration tests that don't need Neo4j use @JdbcTest)
